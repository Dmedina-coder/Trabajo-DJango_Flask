# Proyecto Flask y Django

Este proyecto combina Flask y Django en una misma estructura.

## Estructura

- `flask_app/`: Contiene la aplicación Flask.
- `django_app/`: Contiene la aplicación Django.
- `requirements.txt`: Lista de dependencias.

## Instalación

1. Crea un entorno virtual:
   ```bash
   python3 -m venv venv
   source venv/bin/activate